name: Backend CI

on:
  push:
    branches:
      - main
      - claude/memo-book-project-duulu7
    # tests/test_deploy_config.py asserts properties of deploy/ — the env
    # template, the compose file, the bootstrap script. Triggering only on
    # backend/** meant the test guarding those files never ran when those
    # files changed, which is the one moment it exists for.
    paths: &watched
      - "backend/**"
      - "deploy/**"
      - ".github/workflows/backend-ci.yml"
  pull_request:
    paths: *watched

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: memobook
          POSTGRES_PASSWORD: memobook
          POSTGRES_DB: memobook
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U memobook"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      TEST_DATABASE_URL: postgresql+asyncpg://memobook:memobook@localhost:5432/memobook
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Ghostscript (CMYK conversion tests)
        run: sudo apt-get update -qq && sudo apt-get install -y -qq ghostscript

      - name: Install
        run: pip install -e ".[dev]"

      - name: Lint
        # scripts/ too: it is linted locally and shipped in the image, so
        # leaving it out of CI meant the two disagreed about what passes.
        run: ruff check app tests scripts

      - name: Migrations run against real Postgres (the deploy boot command)
        env:
          DATABASE_URL: postgresql+asyncpg://memobook:memobook@localhost:5432/memobook
        run: |
          alembic upgrade head
          alembic check
          alembic downgrade base

      - name: Test
        run: pytest -q
